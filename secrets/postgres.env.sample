# PostgreSQL Database Secrets
# Copy this file to postgres.env and fill in actual values

# PostgreSQL superuser password
POSTGRES_PASSWORD=YOUR_POSTGRES_PASSWORD_HERE

# PostgreSQL database name (usually matches what's in SQLALCHEMY_DATABASE_URI)
POSTGRES_DB=morphic

# PostgreSQL user (usually matches what's in SQLALCHEMY_DATABASE_URI)  
POSTGRES_USER=morphic